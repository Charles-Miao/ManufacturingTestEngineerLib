```mermaid
graph TD
    A[开始] --> B[设置初始目录并变量]
    B --> C[显示设置LAN为第一启动项的提示]
    C --> D[尝试设置USB LAN为第一启动项]
    D --> E{检查启动顺序是否设置成功?}
    E -->|是| G[结束]
    E -->|否| F[再次尝试设置]
    F --> H{重试次数是否达到上限?}
    H -->|是| I[设置失败提示]
    H -->|否| C
    I --> I[持续显示设置失败提示]

